name: PR Checks

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test & Format (Elixir ${{ matrix.elixir }} / OTP ${{ matrix.otp }})
    runs-on: ubuntu-24.04

    strategy:
      matrix:
        include:
          - elixir: "1.19.5"
            otp: "28.4.1"

    services:
      postgres:
        image: postgres:18.3-alpine
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    defaults:
      run:
        working-directory: ./app

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ matrix.elixir }}
          otp-version: ${{ matrix.otp }}

      - name: Restore dependencies cache
        id: deps-cache-restore
        uses: actions/cache/restore@v4
        with:
          path: ${{ github.workspace }}/app/deps
          key: ${{ runner.os }}-mix-${{ hashFiles('app/mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-

      - name: Restore build cache
        id: build-cache-restore
        uses: actions/cache/restore@v4
        with:
          path: ${{ github.workspace }}/app/_build
          key: ${{ runner.os }}-build-${{ matrix.otp }}-${{ matrix.elixir }}-${{ hashFiles('app/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ matrix.otp }}-${{ matrix.elixir }}-

      - name: Install dependencies
        run: mix deps.get

      - name: Check formatting
        run: mix format --check-formatted

      - name: Compile with warnings as errors
        run: mix compile --warnings-as-errors
        env:
          MIX_ENV: test

      - name: Run tests
        run: mix test
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/app_test
          MIX_ENV: test

      - name: Save dependencies cache
        uses: actions/cache/save@v4
        if: always()
        with:
          path: ${{ github.workspace }}/app/deps
          key: ${{ runner.os }}-mix-${{ hashFiles('app/mix.lock') }}

      - name: Save build cache
        uses: actions/cache/save@v4
        if: always()
        with:
          path: ${{ github.workspace }}/app/_build
          key: ${{ runner.os }}-build-${{ matrix.otp }}-${{ matrix.elixir }}-${{ hashFiles('app/mix.lock') }}
